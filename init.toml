[pre_start]
  cmd = "sleep 2"
  user = "www-data"

[[process]]
  cmd = "sleep 30"
  user = "www-data"

[[process]]
  cmd = "sleep 35"
  user = "dz0ny"

[post_start]
  cmd = "sleep 10"
  user = "www-data"

[[cron]]
  schedule = "@every 5s"
  [cron.job]
    cmd = "echo tick5"
    user = "www-data"

[[cron]]
  schedule = "@every 6s"
  [cron.job]
    cmd = "bash -c 'exit 1'"
    user = "www-data"
